`加入队列接口异常，进行移除，${err.msg || err.errMsg}`（err.msg和err.errMsg是返回的错误信息，可能会没有）调用加入队列接口，如返回success为false时候会有这个文案

操作时间结束，已被移出队列，重新请求加入队列  =>> 倒计时结束后弹窗用户点击加入队列

操作时间结束，被移出队列   =>>> 前端倒计时结束，调用移除队列接口

用户手动返回IP页面

用户手动点击箭头切换了抽赏套数

用户点击换一套随机切换套数

用户长时间停留在赏品规格选择弹窗未作出选择且排队时间过期，被移出队列

前端调用了onTipsClose方法，调用参数flag为：${flag}`    =>>> 关闭弹窗时候可能会触发，如有出现可以把flag让前端看下

获取队列调用新增 loginToken 参数
移除队列调用新增 loginToken 参数
获取IP列表调用新增 loginToken 参数
获取当前IP系列调用新增 loginToken 参数